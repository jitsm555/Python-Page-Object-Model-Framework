from selenium.webdriver.common.by import By

"""
This class contains all common ID's of view pager page, with these id's we are executing tests 

"""


class ViewPagerLocator:
    """ It contains all locators related to view pager"""
    VIEW_PAGER_CONTAINER = (By.ID, 'container')
